package goramda

/*
import (
	"fmt"
	"reflect"
	"testing"
)

func TestGt(t *testing.T) {
	var ttest, ee interface{}
	ttest = 5
	ee = 6
	elmType := reflect.TypeOf(5)
	fmt.Println(ttest, "Element Type:", elmType)

	// f, ok := ttest.(elmType)
	// fmt.Println("Type:", f, ok)
	data := []struct {
		name           string
		firstElement   interface{}
		secondElement  interface{}
		expectedResult bool
		hasError       bool
	}{
		{
			name:           "Int compare",
			firstElement:   5,
			secondElement:  6,
			expectedResult: true,
			hasError:       false,
		},
		{
			name:           "string compare",
			firstElement:   "5",
			secondElement:  "6",
			expectedResult: true,
			hasError:       false,
		},
	}
	fmt.Println(len(data))
	t.Fatal("")
	// for _, d := range data {
	// 	ok, err := Gt(d.firstElement, d.secondElement)
	// 	if ok != d.expectedResult {
	// 		t.Fatalf("Unexpected result expected (%v) got :%v", d.expectedResult, ok)
	// 	}
	//
	// 	if (err != nil) != d.hasError {
	// 		t.Fatalf("Unexpected failure got error :%v", err)
	// 	}
	// }
}
*/
